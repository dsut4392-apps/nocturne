# Rust build artifacts
/target/
**/*.rs.bk
*.pdb

# Cargo lock file (uncomment if this is a library, keep if this is a binary)
# Cargo.lock is typically committed for binaries but not for libraries
# Since this has cdylib/staticlib/rlib, keeping it for reproducible builds

# Cargo cache
.cargo/

# Generated files
*.o
*.so
*.dylib
*.dll
*.a
*.lib

# WASM build artifacts
*.wasm
/pkg/
/wasm-pack.log

# FFI generated header (kept for reference, regenerate with cbindgen)
# include/oref.h is tracked in git for Swift/iOS integration convenience

# IDE / Editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Profiling and debugging
flamegraph.svg
perf.data
perf.data.old
*.profdata

# Test coverage
*.profraw
/coverage/
/tarpaulin-report.html

# Benchmark results
/target/criterion/

# Documentation build
/target/doc/

# Local configuration
.env
.env.local
